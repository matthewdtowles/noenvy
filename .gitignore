# Build artifacts
/noenvy
/dist/

# Local apt-repo signing key material (must never be committed)
/.apt-signing-key/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code per-user settings (project settings.json is fine to commit)
.claude/settings.local.json
.claude/pr-review-fix-state.json

# noenvy's own test artifacts (never commit secrets)
.env
.env.*
!.env.example
